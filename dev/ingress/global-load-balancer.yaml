apiVersion: v1
kind: Service
metadata:
  name: global-load-balancer
  namespace: default
spec:
  selector:
    app: global-ingress
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP